// Declare uma variável contendo uma string
var strind = "String";
// Declare uma variável contendo uma número dentro de uma string
var numString = "2018";
// Declare uma variável com a sua idade
var minhaIdade = 25;
// Declare duas variáveis, uma com seu nome
// e outra com seu sobrenime e some os mesmos
var nome = "Gustavo";
var sobreNome = "Felix";

var nomeCompleto = `${nome} ${sobreNome}`;

// Coloque a seguinte frase em uma variável: It's time

var frasee = "It's time";

// Verigique o tipo de variável que contém o seu nome

console.log(nomeCompleto);
console.log(frasee);
